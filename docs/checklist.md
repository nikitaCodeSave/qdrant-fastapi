# Промпт для проверки интеграции Qdrant в FastAPI

```markdown
# 🔍 Аудит интеграции Qdrant в FastAPI проект

## Контекст задачи
Проведи комплексную проверку добавления Qdrant (векторная база данных) в существующий FastAPI проект. Это критически важный этап — на этой основе будет строиться работа с документами, чанкирование и эмбеддинги. Ошибки на этом этапе создадут технический долг.

---

## 🎭 ЭТАП 1: Архитектурный ревью (роль: Senior Architect)

### 1.1 Структура проекта
Проверь размещение компонентов Qdrant в структуре проекта:

```
Ожидаемая структура:
src/
├── infrastructure/
│   └── qdrant/
│       ├── __init__.py
│       ├── client.py          # Клиент подключения
│       ├── collections.py     # Управление коллекциями
│       └── config.py          # Конфигурация
├── repositories/
│   └── vector_repository.py   # Абстракция над Qdrant
├── services/
│   └── embedding_service.py   # (заглушка для будущего)
└── schemas/
    └── vector_schemas.py      # Pydantic модели
```

**Чеклист:**
- [ ] Qdrant-код изолирован в отдельном модуле (не размазан по проекту)
- [ ] Соблюдается Dependency Inversion — бизнес-логика не зависит напрямую от Qdrant
- [ ] Есть абстрактный интерфейс/протокол для vector store (для возможной замены на другую БД)
- [ ] Конфигурация вынесена из кода (env variables, settings)

### 1.2 Согласованность с существующим проектом
```bash
# Изучи существующие паттерны проекта
cat pyproject.toml
find . -name "*.py" -path "*/repositories/*" | head -5
find . -name "*.py" -path "*/services/*" | head -5
```

**Проверь:**
- [ ] Naming conventions совпадают с остальным проектом
- [ ] Структура классов соответствует существующим паттернам
- [ ] Используются те же подходы к DI что и в проекте
- [ ] Import style согласован (absolute vs relative imports)

---

## 🧪 ЭТАП 2: Code Quality Review (роль: Senior Python Developer)

### 2.1 Клиент Qdrant
Найди и проанализируй код клиента:

```bash
find . -name "*.py" | xargs grep -l "QdrantClient\|qdrant_client" 
```

**Чеклист качества клиента:**
- [ ] Singleton/Factory pattern для клиента (не создаётся на каждый запрос)
- [ ] Graceful shutdown — закрытие соединений при остановке приложения
- [ ] Health check endpoint или метод проверки соединения
- [ ] Retry logic для нестабильных соединений
- [ ] Timeout настройки выставлены явно

**Пример эталонного кода:**
```python
from qdrant_client import AsyncQdrantClient

class QdrantClientManager:
    _client: AsyncQdrantClient | None = None
    
    @classmethod
    async def get_client(cls) -> AsyncQdrantClient:
        if cls._client is None:
            cls._client = AsyncQdrantClient(
                host=settings.qdrant_host,
                port=settings.qdrant_port,
                timeout=settings.qdrant_timeout,
            )
        return cls._client
    
    @classmethod
    async def close(cls) -> None:
        if cls._client:
            await cls._client.close()
            cls._client = None
```

### 2.2 Работа с коллекциями
**Чеклист:**
- [ ] Создание коллекции идемпотентно (recreate=False или проверка существования)
- [ ] Параметры коллекции (vector size, distance metric) вынесены в конфиг
- [ ] Есть возможность задать кастомный payload index
- [ ] Миграции/версионирование схемы коллекций продумано

### 2.3 Type Safety & Pydantic
```bash
# Найди все Pydantic модели связанные с векторами
find . -name "*.py" | xargs grep -l "BaseModel" | xargs grep -l -i "vector\|qdrant\|embed"
```

**Чеклист:**
- [ ] Все входные/выходные данные типизированы через Pydantic
- [ ] Vector dimensions валидируются (Field(min_length=..., max_length=...))
- [ ] Payload схемы определены явно
- [ ] Используется Pydantic v2 синтаксис (model_validate, model_dump)

---

## 🛠️ ЭТАП 3: Infrastructure Review (роль: DevOps Engineer)

### 3.1 Конфигурация
```bash
find . -name "*.py" | xargs grep -l "qdrant" | xargs grep -i "settings\|config\|env"
```

**Минимальный набор настроек:**
```python
class QdrantSettings(BaseSettings):
    qdrant_host: str = "localhost"
    qdrant_port: int = 6333
    qdrant_grpc_port: int = 6334
    qdrant_timeout: float = 30.0
    qdrant_prefer_grpc: bool = True
    
    # Для будущего
    collection_name: str = "documents"
    vector_size: int = 1024  # зависит от модели эмбеддинга
    
    model_config = SettingsConfigDict(env_prefix="")
```

**Чеклист:**
- [ ] Все параметры подключения через environment variables
- [ ] Есть defaults для локальной разработки
- [ ] Разные настройки для dev/staging/prod

### 3.2 Error Handling
**Чеклист:**
- [ ] Кастомные exceptions для Qdrant ошибок
- [ ] Логирование ошибок с контекстом
- [ ] Не проглатываются исключения молча
- [ ] Graceful degradation — что происходит если Qdrant недоступен?

```python
class QdrantConnectionError(Exception):
    """Ошибка подключения к Qdrant"""
    pass

class QdrantCollectionError(Exception):
    """Ошибка операций с коллекцией"""
    pass
```

### 3.3 Docker & Infrastructure
```bash
cat docker-compose.yml | grep -A 20 qdrant
```

**Чеклист:**
- [ ] Qdrant добавлен в docker-compose.yml
- [ ] Volume для персистентности данных
- [ ] Health check настроен
- [ ] Порты не конфликтуют с другими сервисами
- [ ] Ресурсные лимиты заданы (memory limits)

---

## 📚 ЭТАП 4: Documentation Review (роль: Tech Writer)

### 4.1 Код документирован
**Чеклист:**
- [ ] Docstrings на всех публичных методах
- [ ] Type hints везде
- [ ] Примеры использования в docstrings для сложных методов
- [ ] Module-level docstrings объясняют назначение модуля

### 4.2 README и инструкции
**Чеклист:**
- [ ] Обновлён README с информацией о Qdrant
- [ ] Инструкция по локальному запуску Qdrant
- [ ] Описаны environment variables
- [ ] Есть примеры API запросов (если есть эндпоинты)

---

## 🔮 ЭТАП 5: Future-Proofing (роль: Tech Lead)

### 5.1 Готовность к следующим этапам
Проверь, что интеграция готова к:

**Чанкирование документов:**
- [ ] Payload schema поддерживает метаданные чанков (chunk_id, doc_id, position)
- [ ] Есть место для хранения оригинального текста
- [ ] Индексы на payload полях для фильтрации

**Эмбеддинг модели:**
- [ ] Vector size параметризован (разные модели = разные размеры)
- [ ] Distance metric можно менять (Cosine/Dot/Euclid)
- [ ] Batch operations поддерживаются для массовой загрузки

**Поиск:**
- [ ] Метод search принимает фильтры
- [ ] Поддержка score_threshold
- [ ] Пагинация реализуема

### 5.2 Расширяемость
```python
# Проверь наличие абстракции для будущей замены
from abc import ABC, abstractmethod

class VectorStore(ABC):
    @abstractmethod
    async def upsert(self, vectors: list[VectorRecord]) -> None: ...
    
    @abstractmethod
    async def search(self, query_vector: list[float], limit: int) -> list[SearchResult]: ...
    
    @abstractmethod
    async def delete(self, ids: list[str]) -> None: ...
```

---

## ✅ ЭТАП 6: Итоговый чеклист

### Критические (блокеры):
- [ ] Клиент корректно инициализируется и закрывается
- [ ] Конфиг через env variables
- [ ] Базовая типизация есть

### Важные (должны быть исправлены):
- [ ] Error handling реализован
- [ ] Код изолирован в отдельном модуле
- [ ] Docker-compose обновлён
- [ ] Есть health check

### Желательные (технический долг если нет):
- [ ] Абстрактный интерфейс VectorStore
- [ ] Полная документация
- [ ] Retry logic
- [ ] Метрики/логирование

---

## 📋 Формат отчёта

После проверки предоставь отчёт в формате:

```
## Результаты аудита интеграции Qdrant

### ✅ Хорошо реализовано:
- ...

### ⚠️ Требует доработки:
- [Проблема] → [Решение]

### ❌ Критические проблемы:
- [Проблема] → [Решение]

### 📝 Рекомендации на будущее:
- ...

### Оценка готовности: X/10
```

---

## Дополнительные команды для анализа

```bash
# Найти все TODO/FIXME связанные с Qdrant
grep -r "TODO\|FIXME" --include="*.py" | grep -i qdrant

# Проверить тесты
find . -name "test_*.py" | xargs grep -l -i "qdrant"

# Проверить импорты qdrant по проекту (не должно быть везде)
grep -r "from qdrant_client\|import qdrant" --include="*.py" | grep -v __pycache__
```
```

Готово! Убрал упоминания про секреты и API keys. Нужно ещё что-то скорректировать?
